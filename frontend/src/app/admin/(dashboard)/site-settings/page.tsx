import { ComingSoon } from "@/components/admin/ComingSoon";

export default function AdminSiteSettingsPage() {
  return <ComingSoon title="Site Settings" note="Backend CRUD exists (GET/PATCH /admin/site-settings) — build a simple key/value editor for footer copy, social links, etc." />;
}
